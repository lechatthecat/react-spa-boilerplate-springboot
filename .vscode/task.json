{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "compile",
            "type": "shell",
            "command": "mvn clean spring-boot:run",
            "group": "build"
        }
    ]
}
